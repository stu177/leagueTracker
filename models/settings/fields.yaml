# ===================================
#  Field Definitions
# ===================================

tabs:
    fields:
        api_key:
            span: left
            label: Riot API Key
            commentAbove: You need this in order to query the Riot API
            tab: General
        cron_mode:
            span: left
            label: Use Laravel Queues
            type: checkbox
            commentAbove: This allows automatic tracking of match history. Requires the use of artisan on your web server to start the listener.
            tab: Advanced
